{
  "name": "innerwork",
  "version": "1.0.0",
  "description": "Personal growth, journaling, and relationship building",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["personal-growth", "journal", "wellness", "relationships", "ai"],
  "author": "Toby Sauze",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

